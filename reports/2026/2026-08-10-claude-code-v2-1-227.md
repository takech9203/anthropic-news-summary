# Claude Code v2.1.227 リリース — 期限切れトークン起動時のプラン誤判定修正、claude-code-action の Bash 実行不能修正、スラッシュコマンドメニュー改善

## メタデータ

| 項目 | 内容 |
|------|------|
| 発表日 | 2026-08-10 |
| ソース | Claude Code Changelog |
| カテゴリ | Claude Code アップデート |
| 公式リンク | https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md |

## 概要

Claude Code v2.1.227 (2026 年 8 月 10 日、npm 公開日時: 2026-08-10T20:56:57Z) がリリースされた。本バージョンは修正 3 件と改善 2 件の計 5 項目で構成される、修正中心のメンテナンスリリースである。

主な内容は次のとおり。第一に、期限切れのログイントークンでセッションを開始した場合に、フィーチャーフラグがサブスクリプションプラン情報なしで評価され、Max プランのユーザーに Fable 向け使用量クレジットの有効化を誤って促す問題が修正された。第二に、GitHub ホストランナー上で `claude-code-action` を `allowed_non_write_users` と組み合わせて使用すると、すべての Bash コマンドが失敗する問題が修正された。第三に、UI 面ではスラッシュコマンドメニューの視認性が改善され、ファイル候補表示やアットメンションのサイズチェックにおけるイベントループの停止も削減されている。

## 詳細

### 背景

直前のリリース v2.1.225 および v2.1.226 (2026 年 8 月 8 日) では、`CLAUDE_CODE_OAUTH_TOKEN` の上書き問題や macOS キーチェーン起因の 401 エラー連発など、認証関連の安定性修正が行われた。本リリース v2.1.227 はその流れを継ぎ、期限切れトークンでの起動時にフィーチャーフラグ評価が不正確になる問題に対処している。

また、CI/CD 環境で Claude Code を利用する `claude-code-action` については、書き込み権限を持たないユーザーの利用を許可する `allowed_non_write_users` 設定と GitHub ホストランナーの組み合わせで Bash コマンドが機能しない問題が報告されており、本リリースで修正された。

### 主な変更点

#### 修正

1. **期限切れトークン起動時のフィーチャーフラグ誤評価修正**: 期限切れのログイントークンでセッションを開始すると、フィーチャーフラグがユーザーのサブスクリプションプラン情報なしで評価され、Max プランのユーザーに対して Fable 向け使用量クレジットの有効化を誤って促すことがあった問題を修正

2. **`claude-code-action` での Bash コマンド全滅の修正**: GitHub ホストランナー上で `allowed_non_write_users` を設定した `claude-code-action` において、すべての Bash コマンドが失敗する問題を修正

3. **`/tui` による巻き戻し済み会話の復活修正**: 最初のメッセージより前まで巻き戻した (rewind した) 会話が、`/tui` の実行によって復活してしまう問題を修正

#### 改善

4. **スラッシュコマンドメニューの表示改善**: 青色のハイライトは選択中の行のみに適用され、検索で一致した文字は色変更ではなく太字で表示されるようになった。また、絵文字やアクセント記号付きのコマンド名でもグリフが正しく保持される

5. **パフォーマンス改善**: ファイルが見つからない場合の候補表示 (file-not-found suggestions) と、アットメンションのサイズチェックにおけるイベントループの停止 (event-loop stalls) が削減された

### 技術的な詳細

#### 期限切れトークンとフィーチャーフラグ評価

Claude Code は起動時にフィーチャーフラグを評価し、ユーザーのサブスクリプションプランに応じて機能や案内の表示を切り替える。修正前は、ログイントークンが期限切れの状態でセッションを開始すると、プラン情報 (subscription tier) が取得できないままフラグが評価されるため、Max プランのユーザーが未加入ユーザーと同様に扱われ、Fable モデル向けの使用量クレジット有効化を促すプロンプトが誤って表示されることがあった。修正後は、この状況でプランを無視したフラグ評価が行われなくなった。

#### `claude-code-action` の `allowed_non_write_users`

`claude-code-action` はリポジトリへの書き込み権限を持つユーザーのみに実行を許可するのが既定の動作であり、`allowed_non_write_users` は書き込み権限のないユーザーにも実行を許可するための設定である。修正前は、この設定を GitHub ホストランナー上で使用すると、実行環境の制約との組み合わせによりすべての Bash コマンドが失敗し、ワークフローが実質的に機能しない状態になっていた。本修正により、この構成でも Bash コマンドが正常に実行される。

#### イベントループ停止の削減

Claude Code は Node.js 上で動作する TUI アプリケーションであり、イベントループが長時間ブロックされると入力や描画が停止する。修正前は、存在しないファイルパスに対する候補提示や、アットメンション (`@` によるファイル参照) のサイズチェックの処理でイベントループの停止が発生することがあった。本改善により、これらの処理に起因する体感的な引っかかりが軽減された。

## 開発者への影響

### 対象

- **Max プランのユーザー**: 期限切れトークンでの起動時に、Fable 向け使用量クレジットの有効化を誤って促されることがなくなった
- **`claude-code-action` の利用者**: GitHub ホストランナーで `allowed_non_write_users` を設定している場合、Bash コマンドが失敗する問題が解消された。該当する構成の CI/CD ワークフローでは本バージョンへの更新を推奨
- **TUI ユーザー**: 巻き戻した会話が `/tui` で復活する問題の解消に加え、スラッシュコマンドメニューの視認性向上とイベントループ停止の削減により、日常的な操作の体験が改善された

### 必要なアクション

以下のコマンドで最新バージョンに更新できる。

```bash
# npm でのアップデート
npm update -g @anthropic-ai/claude-code

# Homebrew でのアップデート
brew upgrade claude-code

# 現在のバージョン確認
claude --version
```

**推奨される確認事項:**

- **`claude-code-action` 利用者**: `allowed_non_write_users` を使用しているワークフローで、Bash コマンドが正常に実行されることを確認
- **Max プラン利用者**: 使用量クレジット有効化のプロンプトが表示された場合、まずログイン状態 (トークンの有効期限) を確認

### 移行ガイド (該当する場合)

本リリースに破壊的変更はなく、移行作業は不要である。

## コード例

### claude-code-action での allowed_non_write_users の利用

```yaml
# .github/workflows/claude.yml
# v2.1.227 で修正: GitHub ホストランナーでも Bash コマンドが正常に動作
jobs:
  claude:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          allowed_non_write_users: |
            trusted-contributor
```

## 関連リンク

- [Claude Code Changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- [Claude Code GitHub リポジトリ](https://github.com/anthropics/claude-code)
- [Claude Code ドキュメント](https://docs.anthropic.com/en/docs/claude-code)
- [claude-code-action GitHub リポジトリ](https://github.com/anthropics/claude-code-action)
- [Claude Code v2.1.225 & v2.1.226](./2026-08-08-claude-code-v2-1-225-v2-1-226.md)

## まとめ

Claude Code v2.1.227 は、修正 3 件と改善 2 件で構成される小規模なメンテナンスリリースである。新機能の追加はないが、期限切れトークン起動時の Max プラン誤判定、GitHub ホストランナー上での `claude-code-action` の Bash 実行不能という、影響範囲の明確な問題が修正されている。

特に、`allowed_non_write_users` を設定した `claude-code-action` を GitHub ホストランナーで運用しているチームは、すべての Bash コマンドが失敗する問題の修正を含むため、本バージョンへの更新を推奨する。日常的に TUI を利用するユーザーにとっても、スラッシュコマンドメニューの視認性向上とイベントループ停止の削減による体験改善が見込める。
